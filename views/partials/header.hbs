<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Система бронирования авиабилетов</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Custom CSS -->
    <link href="/css/style.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/">Авиабилеты</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/flights">Рейсы</a>
                    </li>
                    {{#if user}}
                        {{#if (eq user.role 'admin')}}
                            <li class="nav-item">
                                <a class="nav-link" href="/admin/dashboard">Панель администратора</a>
                            </li>
                        {{/if}}
                        {{#if (eq user.role 'operator')}}
                            <li class="nav-item">
                                <a class="nav-link" href="/operator/dashboard">Панель оператора</a>
                            </li>
                        {{/if}}
                        {{#if (eq user.role 'passenger')}}
                            <li class="nav-item">
                                <a class="nav-link" href="/passenger/dashboard">Мои билеты</a>
                            </li>
                        {{/if}}
                    {{/if}}
                </ul>
                <ul class="navbar-nav">
                    {{#if user}}
                        <li class="nav-item">
                            <span class="nav-link">Привет, {{user.name}}</span>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/auth/logout">Выйти</a>
                        </li>
                    {{else}}
                        <li class="nav-item">
                            <a class="nav-link" href="/auth/login">Войти</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/auth/register">Регистрация</a>
                        </li>
                    {{/if}}
                </ul>
            </div>
        </div>
    </nav>
</body>
</html> 